# 只给你的今日小店

一个可以直接用浏览器打开的下单网站，适合发给女朋友点甜品、饮品和小惊喜。

## 使用方式

直接打开 `index.html` 即可使用。购物车和订单会保存在当前浏览器的本地存储里。

## 可以改的地方

- 在 `app.js` 里改商品名称、价格、描述。
- 替换 `assets` 里的图片，可以换成你们自己的照片或商品图。
- 如果要真正收款或通知你，需要再接一个后端接口或表单服务。
